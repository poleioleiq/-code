#include<bits/stdc++.h>
using namespace std;
const  int maxn=1e5+5;
int a[maxn][maxn];
int  vis[maxn];
int used[maxn];
int n;
///如果nxt[i]==0||本应和这个女生匹配的男生找到了另外的(第i个女生没有匹配到。已经是联通的)或者
//nxt[i]等价于地i个女生匹配到的男生
ol Find(int u){
  for(int i=1;i<=m;i++){
    if(!vis[i]&&g[i][u]){
      vis[i]=true;
      if(!)
    }
  }
}
